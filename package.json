{
  "name": "favour-flow-backend",
  "version": "1.0.2",
  "scripts": {
    "start": "cd backend && npm install && node app.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}